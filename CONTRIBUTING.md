# 贡献指南

感谢你的贡献。

1. 创建一个功能分支并保持提交聚焦。
2. 为行为变化补充或更新 XCTest。
3. 在提交前运行 `xcodebuild -project JoyCoding.xcodeproj -scheme JoyCoding test`。
4. 不要提交 DerivedData、构建产物、个人 Xcode 状态或证书。

提交 Pull Request 时，请说明用户可见的变化、测试方式和涉及的 macOS 权限行为。
