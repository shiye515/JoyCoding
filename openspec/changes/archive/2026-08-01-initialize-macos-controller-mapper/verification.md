## 自动化验证

- Debug 构建：通过（`CODE_SIGNING_ALLOWED=NO`）。
- 单元测试：14/14 通过。
- Xcode 静态分析：通过，无代码警告。
- 设备状态：覆盖首次选择、手动切换、非选中/选中/最后设备断开和重复设备去重。
- 按键状态：覆盖按下、松开、重复事件、多键并发、跨设备隔离、未知按键和断开清理。
- 权限流程：覆盖步骤顺序、首次展示持久化、再次打开、初始化不请求权限、显式操作请求权限和引用计数轮询。
- 工程隐私配置：应用沙盒已关闭以支持后续辅助功能事件发送；工程未声明 CoreBluetooth 用途说明或权限。输入监控与辅助功能用途在请求前的应用内向导中说明；macOS 没有对应的 `NSInputMonitoringUsageDescription` 官方 Info.plist key。

## 桌面与硬件验收（2026-08-01）

- 通过：首次启动自动显示权限向导。
- 通过：仅浏览“欢迎 → 输入监控 → 辅助功能 → 完成”不会触发系统授权请求。
- 通过：完成向导后显示上下分区主界面和无设备空状态。
- 通过：真实 `Xbox Wireless Controller` 被 GameController 自动识别，顶部显示 1 台设备，底部生成 17 个标准按键条目。
- 待用户配合：实际按下/松开实体手柄按键并观察高亮。
- 待用户配合：关闭主窗口后从系统菜单栏图标重新打开窗口（桌面自动化无法可靠访问 MenuBarExtra 的系统状态项）。

## 双栏界面自动化验证（2026-08-01）

- 通过：`ControllerState` 现维护独立的 `selectedButtonID`，首次设备、设备切换和选中设备断开均回退到当前设备的首个按键。
- 通过：按下/松开事件只更新 pressed set，不会改变用户的按键选择。
- 通过：Debug 构建、18 个单元测试和 Xcode 静态分析。
- 通过：用户已在运行中的 macOS 应用中确认设备下拉、按键列表选中样式、实时状态和右栏映射占位的视觉交互。
