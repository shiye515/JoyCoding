## 1. 应用外壳与工程准备

- [x] 1.1 将应用入口改为带固定 id 的单一 `Window` scene，配置合理的最小窗口尺寸，并加入关闭最后窗口后继续运行的应用代理行为
- [x] 1.2 增加 `MenuBarExtra` 手柄图标及“打开 JoyCoding”“权限设置…”“退出”操作，验证重复打开只聚焦一个主窗口
- [x] 1.3 检查并补充 Xcode target 的框架链接、隐私用途说明及 Debug 构建配置，确保不声明未使用的 CoreBluetooth 权限

## 2. 手柄领域模型与状态归约

- [x] 2.1 新增轻量 `ControllerDevice`、规范化 `ControllerButton` 和权限无关的设备状态模型，定义稳定 id、展示名称与排序
- [x] 2.2 实现设备添加、用户选择、选中/非选中设备断开及无设备状态的确定性归约逻辑
- [x] 2.3 实现按设备隔离的 pressed set 更新逻辑，保证重复事件幂等并支持多个按钮同时按下

## 3. GameController 输入服务

- [x] 3.1 实现主 actor `ControllerService`，在启动时载入 `GCController.controllers()` 并订阅连接与断开通知，按对象标识去重
- [x] 3.2 为 `GCExtendedGamepad` 的面键、肩键、扳机、方向键、菜单类按钮和摇杆按压安装按下/松开处理器并生成按键描述
- [x] 3.3 为没有 extended profile 的设备实现 `physicalInputProfile` 按钮回退，使用系统元素名称生成稳定条目
- [x] 3.4 在设备断开和服务结束时清空该设备状态并解除输入 handler，验证重连不会出现重复回调或残留高亮

## 4. 主界面

- [x] 4.1 将默认 `ContentView` 替换为上下分区布局，顶部使用可横向滚动的设备卡片列表并展示选中状态
- [x] 4.2 实现底部按键列表，根据当前设备展示有序按钮，并为按下、松开和多键同时按下提供清晰的实时视觉状态
- [x] 4.3 实现无设备提示、设备无可识别按钮提示和选中设备断开后的界面过渡，并补充 SwiftUI Preview 样例数据

## 5. 首次启动权限引导

- [x] 5.1 实现三态权限模型和 `PermissionsManager`，封装输入监控/辅助功能状态查询、用户触发的请求及对应系统设置深链
- [x] 5.2 实现引用计数或等价安全机制的可见期权限轮询，使系统设置中的授权变化能实时反映且引导关闭后停止轮询
- [x] 5.3 实现欢迎、输入监控、辅助功能和完成步骤的首次启动向导，准确说明权限用途并提供完成与暂时跳过操作
- [x] 5.4 使用持久标记控制向导只在首次启动自动显示，并接通菜单栏“权限设置…”再次打开且不叠加重复 sheet 的流程

## 6. 自动化测试与验收

- [x] 6.1 建立或配置单元测试 target，为设备初选、手动切换、各类断开和去重规则添加测试
- [x] 6.2 为按钮按下/松开、多键并发、跨设备隔离和断开清理添加状态模型测试
- [x] 6.3 为权限向导步骤、首次展示标记、跳过/完成和仅由用户操作触发请求添加测试替身与单元测试
- [x] 6.4 运行完整单元测试与 Debug `xcodebuild`，修复所有编译错误和警告
- [x] 6.5 使用至少一个真实 USB 或蓝牙手柄手动验收菜单栏重开窗口、热插拔、设备切换、按键实时高亮及权限设置往返，并记录结果

## 7. 双栏设备与按键配置界面

- [x] 7.1 为当前设备增加独立的按键选择状态，并实现设备切换、按键不可用及设备断开时的确定性回退规则
- [x] 7.2 将主界面替换为可调整的左右双栏布局；左栏顶部使用设备下拉选择，左栏下方使用垂直按键列表
- [x] 7.3 为按键列表增加独立于实时按下状态的选中样式，并保留按下/松开状态的实时可见反馈
- [x] 7.4 实现右栏的按键详情与映射设置占位，以所选按键名称显示只读“映射到”值且不写入或执行映射
- [x] 7.5 为设备切换、按键选择回退和按下状态不抢占选择补充单元测试，并完成 Debug 构建与实际手柄界面验收
